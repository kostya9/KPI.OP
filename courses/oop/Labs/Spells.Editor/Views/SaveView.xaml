<UserControl x:Class="Spells.Editor.Views.SaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Spells.Editor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal" Width="300px" Height="100px">
            <Label VerticalAlignment="Center">Export path</Label>
            <TextBox Width="100px" Height="20px" Text="{Binding PathToExportFile, Mode=TwoWay}"></TextBox>
            <Button Height="20px" Command="{Binding ExportCommand}">Export</Button>
        </StackPanel>
    </Grid>
</UserControl>
